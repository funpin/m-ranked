# Migration traceability matrix

This matrix is a live release gate, not a claim that the migration is complete.
`Candidate` means code exists but the route must remain on legacy until its
functional, data, performance, accessibility, and visual evidence is accepted.

| Legacy route/use case | Target API | Target page or adapter | Primary automated evidence | Strangler state | Current gate |
|---|---|---|---|---|---|
| `GET /` overview, filters and periods | `GET /api/v1/overview` | `frontend/app/(overview)/page.tsx` | `PublicQueryControllerTest`, `ProjectionSqlTest`, `frontend/tests/params.test.ts`, V5 golden SQL | Phase-1 candidate | **Blocked:** global sorting, legacy subscribers/posts/M-Rating/trends, and visual parity remain open. |
| `GET /rating` activity ranking | `GET /api/v1/rating` | `frontend/app/rating/page.tsx` | `ActivityRatingQueryTest`, `ProjectionSqlTest`, `ActivityRatingPostgresIntegrationTest`, controller/client/rating parameter tests | Phase-2 candidate; keep legacy until route evidence passes | Telegram channel and VK/RUTUBE institution metrics, platform-specific missing-value order, and top-50 publications are implemented. **Blocked:** production-like visual/performance acceptance remains open, and the target entity response intentionally caps the legacy unbounded list at 200 (`entitiesTruncated`). MAX/all retain the legacy pending state. |
| `GET /compare` fixed cohort | `GET /api/v1/compare` | `frontend/app/compare/page.tsx` | `PublicQueryControllerTest`, `ProjectionSqlTest`, frontend API/parameter tests, real-PG comparison test | Phase-2 candidate | **Blocked:** exact selector correction is in verification; the companion conversion/engagement curve and visual parity remain open. |
| `GET /channels/{id}` | `GET /api/v1/accounts/{id}?legacyType=channels` | `frontend/app/channels/[id]/page.tsx` | Account controller/client tests | **Legacy fallback** | **Blocked:** bounded publication list, revision-pinned account configuration/history, and visual/detail parity are missing. |
| `GET /platform-accounts/{id}` | `GET /api/v1/accounts/{id}?legacyType=platform_accounts` | `frontend/app/platform-accounts/[id]/page.tsx` | Account controller/client tests | **Legacy fallback** | **Blocked:** bounded publication list, revision-pinned account configuration/history, and platform detail parity are missing. |
| `GET /institutions/{id}` | `GET /api/v1/institutions/{id}` | `frontend/app/institutions/[id]/page.tsx` | Institution controller/client tests; `DetailFallbackPostgresIntegrationTest` | **Legacy fallback** | Existing aliases now remain visible with nullable empty metrics instead of a false 404 when the selected revision/platform/period has no aggregate. **Blocked:** account/publication children, 0/1/many-account redirect behavior, revision-pinned catalog identity, and visual parity are missing. |
| `GET /posts/{id}` | `GET /api/v1/publications/{id}?legacyType=posts` | `frontend/app/posts/[id]/page.tsx` | Publication controller/client tests; `DetailFallbackPostgresIntegrationTest` | **Legacy fallback** | Snapshotless aliases now remain visible with nullable counters instead of a false 404. **Blocked:** same-snapshot history, reaction breakdown, neighbours, archived text, chart, revision-pinned identity/configuration, and visual parity are missing. |
| `GET /platform-posts/{id}` | `GET /api/v1/publications/{id}?legacyType=platform_posts` | `frontend/app/platform-posts/[id]/page.tsx` | Publication controller/client tests; `DetailFallbackPostgresIntegrationTest` | **Legacy fallback** | Snapshotless aliases now remain visible with nullable counters instead of a false 404. **Blocked:** raw history/evidence, same-snapshot semantics, revision-pinned identity/configuration, and visual parity are missing. |
| `GET /manage` status and account enable/disable | `GET /api/v1/admin/jobs/**`, `GET /api/v1/admin/platform-accounts/{id}`, `PUT .../enabled` | `frontend/app/manage/**` | Admin controller/security/PostgreSQL tests and frontend admin-client tests | **Legacy fallback** | Partial target slice only; add/update/delete/native-ID/M-Rating commands remain legacy. |
| `GET /health` | `GET /api/v1/health/live`, `GET /api/v1/health/ready` | N/A | Health/readiness tests | **Legacy fallback** | Target probes exist; latest raw-to-published revision freshness is being made fail-closed. |
| `GET /emoji/{emoji_id}` | `GET /api/v1/emoji/{emojiId}` | `frontend/app/emoji/[[...emojiPath]]/route.ts` legacy facade | Spring service/controller/gateway/security tests; `frontend/tests/emoji-route.test.ts`; OpenAPI contract test | **Legacy fallback** until its independent route gate | Candidate preserves ASCII ID bounds, exact legacy 404/500 bodies, framework 404 for missing/slash paths, image bytes/MIME fallback, six-hour cache, redirect host allowlist, ten-second deadline and 2 MB bound. **Blocked:** production-like Telegram/network acceptance, deployment-level egress enforcement plus DNS/IP-rebinding verification required by the threat model, and an explicit route decision remain open; no Nginx switch is authorized. |
| `GET /export/snapshots.csv` | none | none | `tests/test_legacy_csv_contract.py` byte-level characterization; Nginx fallback guard | **Legacy fallback** | **NO-GO on V1-V8:** exact rows require raw same-snapshot counters, deltas, reaction JSON and generic raw JSON unavailable to `api_read`; no inexact target `200` is exposed. A separately reviewed additive compatibility projection/evidence migration is required. |
| `GET /export/posts.csv` | only the non-compatible `GET /api/v1/exports/publications.csv` exists | none | legacy byte-level characterization plus Spring streaming/NULL-zero/BOM/formula-neutralization tests | **Legacy fallback** | **NO-GO on V1-V8:** Telegram needs `max(delta_total)` and its snapshot age; generic mode needs all nullable counters from one latest snapshot, while `publication_latest` independently carries metrics forward. Exact filename/header/query/CRLF semantics are now frozen by tests; the separate modern export prefixes cells beginning with `=`, `+`, `-`, or `@`, without changing legacy bytes or routing. |
| Legacy `/manage/**` POST commands | only optimistic account enabled-state mutation exists | target admin console | Admin audit/CSRF/RBAC/PostgreSQL tests | **Legacy fallback** | Institution/account CRUD, delete, native-ID and official M-Rating refresh remain open. |
| Target collector commit → public read and bounded PG→legacy rollback projection | internal projection rebuild request/publisher plus `operations/bin/pg-to-legacy-sync` | operations publisher/reverse-sync services | Python/PostgreSQL collector tests, strict collector evidence sealer/verifier tests, state-v3 durability/crash tests, operational fail-closed tests and strict-v3 `reverse-sync-rehearsal-v8-local-v3.json` | Writer Gate W | Local V1-V8 four-platform round trip passes with zero identity/snapshot mismatches and duplicate observations. The local report is deliberately non-approvable. The old four-boolean collector artifact is rejected; report v1 now binds fresh protected raw captures to the exact active deploy/Flyway/symlink identity and dedicated external approval, while an independent reviewer still owns the truth of the live-provider/SQL facts. A shared root-owned transition lock now serializes the five privileged entrypoints and passes local contention, nested-call, stale-copy and hostile-environment tests. **NO-GO for production writer cutover** until this exact active release has accepted production-like reverse and collector reports plus every restore/WAL/readiness gate, the lock/ownership contract is rehearsed on the production-like Linux host, and old installed guardless entrypoints are prohibited. |

The authoritative legacy parameter, redirect, CSV and form contract remains
[`migration/baseline/legacy-contract-matrix.md`](baseline/legacy-contract-matrix.md).
No row above authorizes a production route switch or data deletion.
