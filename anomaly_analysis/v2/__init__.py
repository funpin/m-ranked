"""Анализ аномальной динамики v2.

Живёт рядом со старым пакетом, пока не заменит его работника. Читает ряды
через представления `ingest` и `catalog`, пишет только в свои таблицы
`analytics`; импорт API, сборщиков и переноса запрещён тестом изоляции.
"""
