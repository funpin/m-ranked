# M-Ranked frontend

Raw visual captures are local/CI artifacts. Historical summaries remain in Git;
their raw files and checksums refer to the archived run, not a fresh checkout.
See [validation artifact storage](../docs/validation-artifacts.md) for retrieval.

Next.js 16 App Router reads the typed public Spring API; it never reads SQLite or PostgreSQL.

This is alpha's only shipped interface. Logo and favicon are owned by
`frontend/assets/`; no runtime or build dependency on the removed Python UI remains.
Legacy comparison commands below require an explicit
[`MRANKED_LEGACY_REFERENCE_ROOT`](../migration/legacy-reference.md) checkout.

## Runtime and cache

- Node 24, pnpm 11.19.0; install with `pnpm install --frozen-lockfile`.
- `pnpm typecheck` runs `next typegen` before TypeScript, so clean checkouts generate route types without a prior build. `next-env.d.ts` is generated by Next; diagnostic build directories are not checked into the type configuration.
- `API_BASE_URL` is the server-only Spring origin (default `http://127.0.0.1:8080`).
- `SITE_ORIGIN` controls absolute metadata and defaults to `https://m.funpin.org`; HTTPS origin only.
- Public reads use the generated `openapi-fetch` transport. `pnpm generate:api` regenerates types from `contracts/openapi/m-ranked-v1.yaml`; `pnpm check:api` fails on any schema/hash/output drift.
- Every public Next cache selection first reads `/api/v1/revision` with `no-store`. Cache keys include origin, endpoint, complete canonical query, published PG revision and API representation version. A revision race cannot populate the earlier cache key. This is authoritative revision validation; it does not claim delivery of a Pub/Sub invalidation event to Next.
- `NEXT_PUBLIC_DATA_CACHE=disabled` bypasses the Next data cache and retains HTTP ETag revalidation. Admin/auth/exports are excluded from both public Next cache keys and SSR credentials.
- `NEXT_DIST_DIR=.next-visual` isolates a visual development server from the normal build/test directory. Production uses `output: standalone`; copy `.next/static` beside its server.

## Canonical detail routing

All platforms share `/accounts/{uuid}` and `/publications/{uuid}`. Public API
account/publication detail, list and history endpoints accept the same UUIDs
without `legacyType`; numeric IDs retain the old namespace contract. Legacy
`/channels`, `/platform-accounts`, `/posts` and `/platform-posts` detail URLs
redirect with 308 and preserve viewing parameters. Internal detail links,
metadata and previous/next navigation use UUIDs. Institution pages keep their
aggregation role; a single selected-platform account redirects to its UUID.
Canonical account loading also includes Telegram on the all-platform institution
page, without selecting an old namespace from the page's platform filter.

Historical visual/HTTP parity reports below describe the previous URL contract.
Their old status/redirect expectations do not establish acceptance of this change.

## Shared publication charts

All four platforms use `PublicationMeasurements` with one metric definition list.
Available observations determine which counters and reaction breakdown columns
appear; platform-specific wording and Telegram's minimum-people estimate remain
presentation options. Accumulation tooltips include the reaction/like-to-view
percentage from that exact observation. Growth bars, pointer tooltips, keyboard
text and table cells use the stored interval delta, including negative corrections
and NULL; chart sampling and zoom never recalculate a delta between displayed points.

The history table starts with the same bounded view on every platform. Selecting
a chart point with a click or Enter/Space expands the table far enough to include
its exact snapshot, scrolls to the row and focuses its timestamp. Show-all/collapse
controls run locally against the already loaded history, without a 1,000-row
ceiling on revealing a point. Changing publication resets range and selection.

## Compatibility implementation

- Overview keeps repeated scalar last-value semantics, raw Unicode query validation, sort/direction fields, platform autosubmit and browser history restoration.
- Header destinations retain the selected platform. Canonical detail identity supplies platform on SSR and subsequent client navigation.
- Comparison uses `/compare/candidates` and continuation over bounded 50-series responses, never substitutes missing selections. Telegram channel IDs and institution IDs retain their distinct namespaces. Default VK/Rutube selections are populated. A single legend controls both locally imported `@mranked/legacy-chart@0.1.0` plots (workspace dependency pinned in the lockfile); keyboard focus exposes point values and sample sizes. MAX/all retain the actual legacy pending branch.
- Rating continues beyond 200 entities, preserving global row positions and canonical query. A stale cursor offers a restart from the new revision. Sort links preserve browser scroll.
- Account publication lists, institution accounts and publication history use bounded dedicated endpoints with revision-consistent continuation. Institution zero/one/many routing uses the shared UUID account route. Histories expose same-observation counters, corrections, reaction breakdown, archive text when provided, links to neighbours, 144-point chart sampling, time-range controls, shared/independent scales and persisted legacy chart preferences. Chart sampling does not remove observations from the history table. Reaction chips and signed deltas use the API’s retained ordered entries; an unavailable stored delta stays unknown instead of being inferred from adjacent canonical observations.
- `/posts` and `/publications` validate `history_limit` from 50 through 3000 exactly at the HTTP boundary. The default response stays at 100 rows; a visible link requests the bounded full history when older rows exist. Unknown observations, unsupported metrics and absent archive text remain absent; quality metadata does not borrow another metric's sample.
- `/manage` renders the legacy catalog forms and tables through the generated private catalog API. Its per-request reader follows institution and nested account continuation and forwards only incoming Authorization/Cookie with `no-store`. The server facade verifies the session and supplies role flags and CSRF tokens; credentials never enter URLs, persistent storage, SSR markup or client component props. Forms retain legacy actions and include CSRF, correlation IDs and optimistic row versions, including all account versions for the account matrix. Unknown integration, M-Rating and storage status is displayed explicitly. Actual database mutations, conflict handling and authorization remain separate integration gates from the browser fixture tests.

## Reproducible checks

`pnpm check` runs lint, typecheck, OpenAPI drift, unit tests, browser tests,
production build and bundle budgets. It provisions its own browser API fixture
and does not require a running legacy server. Use Node 24 and the pinned pnpm;
install Chromium once with `pnpm exec playwright install chromium`.

The required real PostgreSQL/Spring comparison runs separately through
`migration.integration.run --semantic-only`; see the
[integration instructions](../migration/integration/README.md). It compares all
overview statuses and resolves legacy destinations to their current canonical
identities. `pnpm test:visual` remains an explicit historical pixel audit and is
excluded from `pnpm check`: the current UUID routes and shared publication charts
intentionally differ from the old Python UI. Historical reports below retain
their original scope and results.

Playwright starts an explicit contract-shaped API test double on 18091 and a fresh standalone production Next build on 18092. It checks interactions, validation, continuation, history, back/forward, menu, theme, private catalog role/form behavior, cache headers and axe AA at 1440×900 and 390×844. That run proves frontend behavior, not PG mutations or legacy numeric parity. Reports are under `test-results/`; `PLAYWRIGHT_REPORT` selects a distinct report file. `PLAYWRIGHT_PRODUCTION=false` enables the development server for diagnostics, but its SSR cache-header behavior cannot establish the private production cache gate.

Create a new disposable representative corpus (refuses an existing destination):

```
../.venv/bin/python scripts/legacy-fixture.py --database /tmp/mranked-review.sqlite
../.venv/bin/python scripts/legacy-fixture.py --serve --database /tmp/mranked-review.sqlite --port 18080
```

The manifest records SHA256, fixed clock and row counts. Import that same file through the actual bridge into a disposable PostgreSQL database, rebuild projections, and freeze its published revision timestamp to the manifest clock before starting the API. Never use production data or rewrite production revision timestamps for a screenshot.

```
LEGACY_BASE_URL=http://127.0.0.1:18080 \
TARGET_BASE_URL=http://127.0.0.1:18084 \
VISUAL_FIXTURE_MANIFEST=/tmp/mranked-review.manifest.json \
VISUAL_OUTPUT=evidence/visual-review pnpm test:visual
```

`--capture-legacy` records a baseline before edits. Default visual coverage spans all five platforms, periods, pending and empty states plus details/invalid/manage URLs at both exact viewports. `VISUAL_ROUTES` explicitly selects a diagnostic JSON subset; a subset is not the full route gate. `VISUAL_WORKERS` defaults to 2 (max 4).

The runner saves real PNG, DOM, ARIA, computed styles, axe output, CLS, pixel diff and provenance. It fails if pixel difference exceeds 0.5%, HTTP status differs, axe violations remain, CLS exceeds 0.1, horizontal overflow occurs, or capture fails. The legacy CDN reference is fulfilled by locally pinned Chart.js 4.4.7 bytes. Target charts use the versioned local Canvas2D package; it has no runtime CDN dependency. `VISUAL_CHART_STATES=true` adds below-fold plots, hover tooltips and both themes; `VISUAL_CHART_VARIANTS` can select diagnostic variants. No image format relabeling or API test doubles are used for visual evidence. Intermediate failures remain failures; generated reports do not authorize changing production routes.

**Instrumentation correction (2026-09-06):** captures produced before instrumentation version 2 used a serialized TypeScript `FrozenDate` class whose transpiled `__name` helper was absent in Chromium. The initialization failed before freezing the browser clock or registering CLS; `visualCls ?? 0` then reported an unmeasured zero. Those historical PNG, pixel-diff, axe, HTTP and explicit status observations are retained, but their claimed frozen browser clock and CLS acceptance are invalid. They do not establish the full visual gate. The [defect record](evidence/visual-instrumentation-defect-20260906/report.json) identifies affected reports and the reproduction. Version 2 introduced plain JavaScript, successful observer initialization and exact browser-clock verification for every document, failing if metrics are missing. Version 3 also installs a legacy-only hook before the original Chart.js assignment to disable still-capture animation before the first constructor; this prevents a frozen wall clock from leaving a long-period chart in a perpetual animation. The original library bytes, data, scales and formatters remain unchanged. Native positive CLS/Event Timing observations are recorded in the [NULL-tooltip probe](evidence/chart-null-tooltip-v29-r2/report.json). Chart stills disable legacy animation only to reach its final endpoint; chart values, axes and tooltip formatting remain unchanged, and performance measurements never disable animation. The probe confirms the visible NULL tooltip difference (`0` in legacy, `—` in target). On 2026-09-06 the user explicitly chose “Оставить „—“ для отсутствующего значения”; the [approval companion](evidence/null-tooltip-approval-20260906.json) records that narrow presentation exception. API values, table values and chart gaps remain NULL; no runtime edit or rebuild was needed. The original probe remains unchanged as evidence of the difference.

The full visual gate also requires authenticated manage overview, account matrix and institution-name editing states. Supply separate `LEGACY_ADMIN_USERNAME`/`LEGACY_ADMIN_PASSWORD` and `TARGET_ADMIN_USERNAME`/`TARGET_ADMIN_PASSWORD` through the local environment, or a mode-0600 JSON file selected by `VISUAL_ADMIN_CREDENTIALS_FILE`; each origin gets an isolated browser authentication context. Anonymous `/manage` retains its own 401 scenario. Without the credential pairs, authenticated coverage is explicitly `NOT_RUN_MISSING_CREDENTIALS` and a full run remains `NO-GO`. The frozen legacy server accepts its explicit `LEGACY_ADMIN_*` variables and `LEGACY_ADMIN_CSRF_SECRET`; it never loads production credentials. Text artifacts redact password/CSRF values, including HTML and RSC escaping; raw screenshots are always unmasked. `VISUAL_STORAGE_MASKS=true` can additionally produce `.comparison.png` captures that mask only paired live storage-size numeric glyphs. Progress indicators, labels, panels, tables and charts stay unmasked. Masks are skipped if vertical layout differs; exact rectangles, reasons and the raw pixel ratio remain in the report.

Mobile performance uses `scripts/mobile-performance.mts` with `PERFORMANCE_BASE_URL`, server-only `API_BASE_URL`, `VISUAL_FIXTURE_MANIFEST`, `PERFORMANCE_RUNTIME_EVIDENCE` (the actual API performance report including database size/revision), `NEXT_DIST_DIR` and `PERFORMANCE_BUILD_MODE=production`. Set `PERFORMANCE_HOST_QUIET_CONFIRMED=true` only after coordinating a host without concurrent build/import/test workloads; otherwise the run cannot establish acceptance. At least five cold browser samples per route measure LCP, exercised Event Timing interactions, CLS session windows and the gzip size of every initial script, including dynamic charts. Both Next firstLoad and initial-network gzip totals are reported; only explicitly marked speculative requests are listed separately. Interactions include theme, keyboard, legend visibility and history scale changes. The report pins source hash, published revision before/after, build ID, API artifact metadata and mobile throttling. Large comparisons reuse bounded groups of rasterized series (at most 16 MiB per chart; small charts use one data plane). A legend change preserves every point and rebuilds only changed groups while axes are unchanged. Workload eligibility verifies manifest-bound row counts (over 200 institutions, at least 800 accounts/publications, 9,000 observations and 8 million comparison points); physical database bytes remain recorded, because fresh imports and rebuild bloat allocate different space for the same corpus. This is a local lab gate and cannot establish production workload equivalence. The representative synthetic corpus is distinguished from production field performance; failing measurements remain release blockers.

The default measurement is ten cold browser contexts for each of five routes (Telegram/all overview, Telegram rating, default VK comparison and Telegram publication history). The producer rejects mismatched source/API/revision evidence before opening a browser. Each successful sample records and verifies the rendered workload: 50 initial overview cards, 200 initial rating entities, both comparison charts with every selected candidate beyond 200, and both publication charts with history rows. An HTTP 200 API fallback is a failure. Every initial script body must also match the SHA-256 of the corresponding file in the declared production artifact. Reports include the producer and shared browser-instrumentation hashes and host CPU/OS/memory metadata; load averages are context, not proof of an idle host. The final run still requires explicit coordination of the quiet window.

A11y differences from the inherited CSS are explicit: inactive light platform labels have sufficient contrast (legacy 4.42:1), disabled post navigation has readable text, history links are underlined, tooltip evidence stays inside card bounds, focus and canvas keyboard descriptions are added. These changes are checked in both themes and participate in unmasked pixel comparisons.

## Release routing

Production remains on the legacy routing phase until the complete route, visual, numeric, authorization, recovery, performance and operational gates pass. The current routing phase intentionally leaves `/manage` on legacy and denies public `/api/v1/admin/**`. An eventual admin phase must forward same-origin HTTPS Authorization/Cookie/CSRF/correlation headers directly to Spring, preserve no-store and RFC 9457 responses, and pass role/CSRF/409/sign-out rehearsals. Do not modify historical gate reports to imply completion.

The [final frontend evidence index](evidence/frontend-final-v29-r2-20260906.json) binds the production build/API/source and records the final 176/176 full visual states plus 98/98 chart states. All captures are unmasked, verify the frozen browser clock and registered CLS observer, and stay below the 0.5% raw-pixel threshold (maxima 0.242% and 0.318%). Authored HTML has zero applicable axe violations; the four native JSON-viewer states retain `document-title`/`html-has-lang` observations, with exact protocol bytes and statuses checked instead. These local gates do not change production routing.

The [final V29 r2 mobile lab report](evidence/mobile-performance-v29-r2/report.json) records 50/50 valid cold samples during the coordinated quiet window. All five route gates passed: maximum route p75 LCP 2,488 ms, INP 152 ms, CLS 0, and initial JavaScript 155,050 bytes gzip including charts. The comparison LCP margin is only 12 ms; this synthetic representative-corpus result does not establish production field performance. Browser instrumentation and artifact hashes, every sample and the unchanged thresholds are retained in the report.

Actual browser mutations use `scripts/manage-flow.mts` with `MANAGE_BASE_URL`, `MANAGE_RUNTIME_EVIDENCE`, the private credential file, and `MANAGE_ALLOW_DISPOSABLE_MUTATIONS=true`. The runtime must identify a dedicated `admin_review_forms_*` database; frozen visual/performance corpora are never mutated. Real editor/admin forms are followed by generated-client API reads, with role capabilities, cancellation, stale versions and invalid CSRF checked. Set `MANAGE_MRATING_OFFLINE_FIXTURE=true` only for an explicitly configured offline rating fixture. Reports record only Origin/Host/status diagnostics, never credentials or CSRF values.
