# M-Ranked frontend

Next.js 16 App Router reads the typed public Spring API; it never reads SQLite or PostgreSQL.

This is alpha's only shipped interface. Logo and favicon are owned by
`frontend/assets/`; no runtime or build dependency on the removed Python UI remains.
The interface uses Tailwind v4, shadcn/Base UI, Geologica headings and Montserrat body text.
Light, dark and system preferences share the existing `data-theme` contract.

## Runtime and cache

- Node 24, pnpm 11.19.0; install with `corepack pnpm install --frozen-lockfile`.
- `pnpm typecheck` runs `next typegen` before TypeScript, so clean checkouts generate route types without a prior build. `next-env.d.ts` is generated by Next; diagnostic build directories are not checked into the type configuration.
- `API_BASE_URL` is the server-only Spring origin (default `http://127.0.0.1:8080`).
- `SITE_ORIGIN` controls absolute metadata and defaults to `https://m.funpin.org`; HTTPS origin only.
- Public reads use the generated `openapi-fetch` transport. `pnpm generate:api` regenerates types from `contracts/openapi/m-ranked-v1.yaml`; `pnpm check:api` fails on any schema/hash/output drift.
- Every public Next cache selection first reads `/api/v1/revision` with `no-store`. Cache keys include origin, endpoint, complete canonical query, published PG revision and API representation version. A revision race cannot populate the earlier cache key. This is authoritative revision validation; it does not claim delivery of a Pub/Sub invalidation event to Next.
- `NEXT_PUBLIC_DATA_CACHE=disabled` bypasses the Next data cache and retains HTTP ETag revalidation. Admin/auth/exports are excluded from both public Next cache keys and SSR credentials.
- `NEXT_DIST_DIR=.next-review-local` isolates a development server from the normal build/test directory. Production uses `output: standalone`; copy `.next/static` beside its server.

## Canonical detail routing

All platforms share `/accounts/{uuid}` and `/publications/{uuid}`. Public API
account/publication detail, list and history endpoints accept the same UUIDs
without `legacyType`; numeric IDs retain the old namespace contract. Legacy
`/channels`, `/platform-accounts`, `/posts` and `/platform-posts` detail URLs
redirect with 308 and preserve viewing parameters. Internal detail links,
metadata and previous/next navigation use UUIDs. Institution pages keep their
aggregation role; a single selected-platform account redirects to its UUID.
Canonical account loading also includes Telegram on the all-platform institution
page, without selecting an old namespace from the page's platform filter.

Historical visual/HTTP parity reports below describe the previous URL contract.
Their old status/redirect expectations do not establish acceptance of this change.

## Shared publication charts

All four platforms use `PublicationMeasurements` with one metric definition list.
Available observations determine which counters and reaction breakdown columns
appear; platform-specific wording and Telegram's minimum-people estimate remain
presentation options. Accumulation tooltips include the reaction/like-to-view
percentage from that exact observation. Growth bars, pointer tooltips, keyboard
text and table cells use the stored interval delta, including negative corrections
and NULL; chart sampling and zoom never recalculate a delta between displayed points.

The history table starts with the same bounded view on every platform. Selecting
a chart point with a click or Enter/Space expands the table far enough to include
its exact snapshot, scrolls to the row and focuses its timestamp. Show-all/collapse
controls run locally against the already loaded history, without a 1,000-row
ceiling on revealing a point. Changing publication resets range and selection.

## Compatibility implementation

- Overview keeps repeated scalar last-value semantics, raw Unicode query validation, sort/direction fields, platform autosubmit and browser history restoration.
- Header destinations retain the selected platform. Canonical detail identity supplies platform on SSR and subsequent client navigation.
- Comparison uses `/compare/candidates` and continuation over bounded 50-series responses, never substitutes missing selections. Telegram channel IDs and institution IDs retain their distinct namespaces. Default VK/Rutube selections are populated. A single legend controls both Recharts SVG plots; keyboard focus exposes point values and sample sizes. Point markers are disabled on comparisons to keep the 207-series workload bounded; focused points still have a marker. Legend state updates immediately, while the plot uses deferred visibility. MAX/all retain the actual legacy pending branch.
- Rating continues beyond 200 entities, preserving global row positions and canonical query. A stale cursor offers a restart from the new revision. Sort links preserve browser scroll.
- Account publication lists, institution accounts and publication history use bounded dedicated endpoints with revision-consistent continuation. Institution zero/one/many routing uses the shared UUID account route. Histories expose same-observation counters, corrections, reaction breakdown, archive text when provided, links to neighbours, 144-point chart sampling, time-range controls, shared/independent scales and persisted chart preferences. Chart sampling does not remove observations from the history table. Reaction chips and signed deltas use the API’s retained ordered entries; an unavailable stored delta stays unknown instead of being inferred from adjacent canonical observations.
- `/posts` and `/publications` validate `history_limit` from 50 through 3000 exactly at the HTTP boundary. The default response stays at 100 rows; a visible link requests the bounded full history when older rows exist. Unknown observations, unsupported metrics and absent archive text remain absent; quality metadata does not borrow another metric's sample.
- `/manage` renders native catalog forms and shadcn tables through the generated private catalog API. Its per-request reader follows institution and nested account continuation and forwards only incoming Authorization/Cookie with `no-store`. The server facade verifies the session and supplies role flags and CSRF tokens; credentials never enter URLs, persistent storage, SSR markup or client component props. Forms retain native POST actions and include CSRF, correlation IDs and optimistic row versions, including all account versions for the account matrix. Deletion uses a lazily loaded AlertDialog, with cancellation restoring focus and confirmation submitting the original form payload. Without JavaScript, forms retain their native submission behavior. Unknown integration, M-Rating and storage status is displayed explicitly. Actual database mutations, conflict handling and authorization remain separate integration gates from the browser fixture tests.

## Reproducible checks

`pnpm check` runs lint, typecheck, OpenAPI drift, unit tests, browser tests,
production build and bundle budgets. It provisions its own browser API fixture
and does not require a running legacy server. Use Node 24 and the pinned pnpm;
install Chromium once with `pnpm exec playwright install chromium`.

The required real PostgreSQL/Spring comparison runs separately through
`migration.integration.run --semantic-only`; see the
[integration instructions](../migration/integration/README.md). It compares all
overview statuses and resolves legacy destinations to their current canonical
identities. Pixel parity with the removed Python UI is no longer an acceptance gate.
The old CSS migration layer and `test:visual` runner have been removed.

Playwright starts an explicit contract-shaped API test double on 18091 and a fresh standalone production Next build on 18092. It checks interactions, validation, continuation, history, back/forward, menu, theme, private catalog role/form behavior, cache headers and axe AA at 1440×900 and 390×844. That run proves frontend behavior, not PG mutations or legacy numeric parity. Reports are under `test-results/`; `PLAYWRIGHT_REPORT` selects a distinct report file. `PLAYWRIGHT_PRODUCTION=false` enables the development server for diagnostics, but its SSR cache-header behavior cannot establish the private production cache gate.

## Chart loading and budgets

`comparison-chart.tsx` keeps the shared legend in the initial bundle;
`publication-measurements.tsx` keeps metric toggles, scale and range controls,
and the history table. The SVG renderers load through `next/dynamic` with
fixed-height accessible skeletons. Both keyboard and pointer selection retain
exact observations and stored deltas. No additional animation library is used.

`corepack pnpm check:bundle` measures per-route firstLoad gzip from Next's
production diagnostics and writes `reports/bundle-budget.json`. The ceilings
are based on the redesign measurements with approximately 15% headroom.
This metric excludes deferred chunks: total initial network JavaScript,
including the renderers, is measured separately by the mobile runner.

## Mobile measurements

The frontend calibration in `scripts/redesign-performance.mts` uses the explicit
`REDESIGN_PERFORMANCE_FIXTURE=true` API test double: 50 overview cards,
200 rating entities, 207 comparison series × 337 hours on each of two charts,
and 160 publication observations. It runs five cold mobile contexts per route
with CPU 4×, 150 ms latency and 1.6 Mbps download. Its report is
`reports/redesign/calibration.json`; this is frontend-only synthetic evidence,
not PostgreSQL or production acceptance. From `frontend/`, use three terminals (the server command builds its own
production artifact). Install dependencies first with the pinned pnpm:

```bash
# Terminal 1
REDESIGN_PERFORMANCE_FIXTURE=true node --import tsx tests/api-fixture.mts
# Terminal 2
API_BASE_URL=http://127.0.0.1:18091 NEXT_PUBLIC_DATA_CACHE=disabled node scripts/browser-server.mjs
# Terminal 3, once Next is ready
NEXT_DIST_DIR=.next-browser-tests node --import tsx scripts/redesign-performance.mts
```

The stricter `scripts/mobile-performance.mts` still requires a real API and
source-bound runtime evidence: `PERFORMANCE_BASE_URL`, `API_BASE_URL`,
`VISUAL_FIXTURE_MANIFEST`, `PERFORMANCE_RUNTIME_EVIDENCE`, `NEXT_DIST_DIR`,
and `PERFORMANCE_BUILD_MODE=production`. Its corpus, revision, artifact hash,
rendered workload, CLS and quiet-host checks remain enforced. Set
`PERFORMANCE_HOST_QUIET_CONFIRMED=true` only for an actually coordinated quiet
window. The visual-prefixed manifest variable is retained for compatibility;
it no longer enables pixel comparison.

Route budgets are recorded in `scripts/performance-budgets.json`. The higher
comparison INP ceiling is an explicit consequence of the accepted SVG renderer
on hundreds of lines; it is not a claim that the page meets field Core Web
Vitals. Existing baseline reports are never retroactively changed to PASS.

## Historical evidence

Python pixel-parity captures, Canvas measurements and their old thresholds
refer to the interface before `2349966` (the shadcn redesign foundation).
They are retired evidence, not checks of the current UI. The former README
and report references remain available in Git at `e029e73`; their stated
limitations, including the early CLS instrumentation defect, remain applicable.
Generated historical captures are not part of the active frontend evidence set.
HTTP behavior, accessible names, canonical routing, data semantics, role/CSRF
checks and browser history are still tested independently of appearance.

## Local production-copy review

From the repository root:

```bash
make stand-prodcopy COPY=~/Downloads/m-ranked-pgcopy ENV=~/Downloads/m-ranked-local/prodcopy.env
```

The helper reuses the dedicated local volume and leaves the source copy intact.
The interface is at `http://localhost:3000`; see
[local stand documentation](../infra/local/README.md) for the database and service
isolation. Running this stand does not change production routing.

Actual browser mutations against a dedicated disposable database use
`scripts/manage-flow.mts` with `MANAGE_BASE_URL`, `MANAGE_RUNTIME_EVIDENCE`,
the private credential file and `MANAGE_ALLOW_DISPOSABLE_MUTATIONS=true`.
The runtime must identify an `admin_review_forms_*` database. Frozen visual or
performance corpora are never mutated. The script checks role capabilities,
confirmation/cancellation, optimistic conflicts, CSRF and subsequent API reads.
