/** Служебные кнопки шапки.
 *
 * Стандартное наведение опирается на --muted, а он в светлой теме почти
 * неотличим от полотна: кнопка выглядела неактивной. Подложка задаётся долей
 * от цвета текста, поэтому одинаково заметна в обеих темах. */
export const HEADER_BUTTON = "size-8 rounded-lg hover:bg-foreground/[0.07] dark:hover:bg-foreground/10";
