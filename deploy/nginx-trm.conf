# Legacy HTTP-only configuration is intentionally reduced to redirects.
# Production HTTPS is configured in m.funpin.org.nginx.conf.
server {
    listen 80;
    listen [::]:80;
    server_name m.funpin.org;
    return 301 https://$host$request_uri;
}
