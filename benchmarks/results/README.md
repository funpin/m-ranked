# Benchmark results

Committed JSONL files are immutable local evidence. Each line is one invocation of the command
documented in `../README.md`; environment fields make cross-host comparisons explicit.
