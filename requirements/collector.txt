# Среда выполнения коллекторов. Telethon, PyMax и lxml подтягиваются лениво,
# но нужны в образе, если платформа включена. PyMax вынесен в
# requirements/pymax.txt: зависимость из git несовместима с проверкой
# хешей и собирается отдельным, отдельно просматриваемым шагом.
psycopg[binary,pool]==3.3.5
httpx==0.28.1
python-dotenv==1.2.3
beautifulsoup4==4.13.4
lxml==6.1.0
Telethon==1.40.0
