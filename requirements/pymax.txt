# Клиент MAX живёт только на GitHub и колёс не публикует.
#
# Из этого следует отдельный порядок: коммит закреплён полным хешем, колесо
# собирается отдельным шагом CI из этого же коммита, его sha256 попадает в
# SBOM и в журнал сборки, а образ ставит уже готовый файл. Так проверка хешей
# остальных зависимостей не отключается ради одной строки.
git+https://github.com/MaxApiTeam/PyMax.git@53103f0c8df32f36110cbe0bcbfc3ad82173808a#egg=maxapi-python
