# Разработка и CI: среды выполнения плюс инструменты проверки.
# Образы ставят не эти файлы, а requirements/*.lock с хешами.
-r api.txt
-r collector.txt
-r pymax.txt
-r telegram-web.txt
pytest==9.1.1
pyyaml==6.0.3
jsonschema==4.26.0
referencing==0.37.0
# Шлюз состава зависимостей: тот же инструмент запускает CI.
pip-audit==2.10.1
