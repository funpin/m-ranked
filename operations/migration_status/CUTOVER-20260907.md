# Production cutover, 2026-09-07 — IN PROGRESS

> The sections below are chronological. The latest explicit scope change and
> updates at the end supersede earlier full-source validation instructions.

## Historical state at 02:46 UTC

Four target collectors were independently installed at
`/opt/m-ranked/releases/collectors-v30-20260907` and verified with actual
PostgreSQL writes and successful cycles (Telegram 66, VK 66, MAX 65, RUTUBE 64
accounts). Collector partition provisioning deadlocks and RUTUBE future
premiere normalization were fixed; 45 targeted tests passed. These code changes
are in the working tree and installed on production.

Target collectors are enabled but currently STOPPED for catch-up consistency.
They MUST be resumed after import/reconciliation and projection readiness.
Telegram Web authorization is missing in both old and copied profiles;
public collection works, but complete Telegram comments require reauthorization.

Legacy collector/web/backup timer are disabled and stopped. Legacy application
runtime and old live metric SQLite were removed only after final source
preservation. Unrelated Amnezia containers were retained.

## Sources and import

Initial source SHA256:
`4061e900f6f2700498795e39d8fa70a076a9f191e3c30b143cb455371c26f80e`.
Initial batch `e832c733-8416-5f15-8649-b11405e49528` has all ten stream
checkpoints complete, 4,175,064 rows committed. Subsequent reconciliation
failed (temporary file limit, then stale projection revision while native
collectors ran). This is NOT final acceptance.

The independently verified final union contains the stopped live SQLite plus
four missing original posts and four missing post_messages. No missing
snapshot rows were found in the old original source. Final source:
`/var/lib/m-ranked/catchup-20260907/final.sqlite`, SHA256
`b670a51a72194641fad06912ea07baf8c7937571801c5be8f0cb2f0332de2b79`,
1,934,426,112 bytes, 4,421,655 source rows, quick_check and FK checks passed.
Original S0 is still mounted from its compressed Squashfs image.
DO NOT remove either source before successful independent verification.

Current batch `1fbb7597-18c3-595c-b6b6-582fee82a158`, source namespace
`m-ranked-production`, snapshot kind `catch_up`, runs under
`m-ranked-backfill.service`. Reports:
`/var/lib/m-ranked/catchup-20260907/work/reports`.
Identity history verification uses S0 plus durable native identity receipts.
Legacy-only exact projection comparison is intentionally not enabled for the
mixed native + legacy dataset; target projection readiness still must pass.
The importer rebuilds all projections and verifies canonical facts.

Before catch-up, native compatibility alias collisions were aligned to final
source IDs without changing canonical UUIDs or imported mappings. Root receipt:
`/var/lib/m-ranked-migration-status/catchup-alias-alignment-20260907.json`.
An independent disk guard stops import before the 3 GiB reserve is exhausted:
`m-ranked-final-progress-20260907.service`.

## Counter correction

The current batch scans the full final source, including previously imported
rows. Checkpoint counts therefore describe the final PASS, not newly added
PostgreSQL records. The publisher now validates a separately bound original
batch and its ten completed checkpoints and exposes `previouslyTransferred`.
It DOES NOT add current pass progress to the original count (double counting).
Page labels explicitly describe final pass progress; the original retained
volume is separate. ETA is disabled for this mode. Collection status explicitly
states the current pause. Updated publisher, HTML, control and guard installed.
19 migration status regression tests passed. HTTPS JSON from production origin
confirmed `counterKind=final_pass`, `previouslyTransferred=4175064`, no ETA.
The local in-app browser could not connect (ERR_CONNECTION_CLOSED), so no
successful browser visual verification is claimed.

## Web/API preparation — NOT PUBLIC, NOT RUNNING

Release `/opt/m-ranked/releases/app-v30-20260907`:
- Backend JAR SHA256
  `fbdd20b3ed5249e951638fb097485f37d65a5c840b4d2e3db47de0c6e0a4f24b`.
- Standalone frontend tar SHA256
  `092b28e1669efd89ab39ac1567444bf745bc4b6812e1c75ebf8499181351f630`;
  extracted successfully and Next 16.3.3 loaded with Node 24.
- Backend package build and 12 security/config tests passed; frontend build
  and type checks passed.

Target API/web systemd units are staged, verified with systemd-analyze, and
not started/enabled. They use pinned official Docker image digests, dedicated
UIDs, read-only code, localhost listeners, bounded memory/CPU. Credentials are
root-managed or private to the API UID, never embedded in release artifacts.
The existing legacy admin login was preserved using bcrypt.
Redis official image is pulled; Redis service not started yet.
Public nginx still serves the migration page on all routes.

## Remaining work

1. Await current batch; diagnose any real verification failure while preserving
   final SQLite and S0. Do not convert failed status to accepted manually.
2. Verify target projections and initialize projection publisher/outbox workers.
3. Resume all four native collectors and confirm fresh writes.
4. Complete source-data acceptance, then retire old stack and migration-only
   sources/images/roles/files. Keep durable identity receipts and new MAX auth
   session (these are new-stack state, not migration debris).
5. Inspect retained historical CSV archives and old-backups.tar.gz for unique
   data before deletion. No off-host backup has been verified present.
6. Start Redis/API/web, verify health, admin auth, public queries, then switch
   nginx /api/ to 8080 and frontend to 3000; verify external routes.
7. Install/verify new backup, maintenance and restore workflows.

Last disk reserve observation: 6.2 GiB available; host has one CPU and 2 GiB RAM.
Do not assume unused Docker images or all SQLite files are safe to delete.

## Updates after user continuation (03:10 UTC)

- User confirmed external backup storage exists but access is not configured.
- Native collectors resumed at 02:49 UTC during bulk catch-up. Guard stops
  them when fewer than 500,000 pass rows remain, before final reconciliation.
  Guard is now a persistent systemd unit, not transient.
- Added `migration/bridge/snapshot_reuse.py`: batch eligibility requires exact
  source row hash, actual canonical snapshot and publication/month/bucket,
  retained export lexeme and all required sanitized evidence. Immutable data
  is not reinserted; lineage and evidence are advanced in the checkpoint txn.
  Missing/changed/incompletely preserved rows use normal import.
- Actual isolated PostgreSQL test passed: changed/new rows only use normal
  writer, forced interruption rolls back reused lineage with the checkpoint,
  resumed full canonical reconciliation passes. Schema-only rehearsal DB and
  seed policies used; no production rows copied. Rehearsal DB removed.
- r7 release adds one publication alias lookup per distinct publication in
  each bounded batch; batch size is now 5,000. Existing batch/checkpoints kept.
  Local bridge and counter tests: 34 passed. Latest import restarted 03:10 UTC.
- Counter now reports `counterKind=delta`: original retained 4,175,064 plus
  distinct new source keys first mapped by the final batch. Uses temporary
  aggregate view `migration.final_delta_progress_20260907` and partial index
  `progress_final_new_rows_20260907`; remove these during migration cleanup.
  No double-counting of replayed checkpoints. At 03:03 UTC: 32 new metadata
  rows, 246,559 snapshots still to add. `passProcessed` is separate diagnostics.
- Auto-review initially rejected deleting the two older recovery files. User
  then explicitly confirmed both named files; deleted old-backups.tar.gz and
  pre-deploy-1690575-20260901T125843Z.db, freeing 614,759,843 bytes. Final SQLite
  and original S0 retained. Root receipt `retired-older-sqlite-20260907.json`.
- Redis is running on loopback with existing credential. Target worker image
  combines pinned official PostgreSQL 18.6 and Redis 8.10.0 client tools, both
  versions tested. Projection publisher, cache outbox and maintenance units
  staged and systemd-verified, not started.
- API/web are being smoke-tested on private loopback ports only. Public nginx
  remains the maintenance page. Do not assume web/API readiness yet.

## Explicit scope change at 03:22 UTC — authoritative

User explicitly instructed loading ONLY the 246,559 remaining measurements,
waiving revalidation of historical rows to save server time and memory. The
full-source importer and its inventory/replay/reconciliation were stopped.

The active release is `/srv/m-ranked-artifacts/final-delta-20260907`, command
`-m operations.migration_status.import_final_delta` under the same backfill unit.
It does NOT construct BridgeService or call inventory/full reconciliation.
It reads only SQLite rowids above the two completed original stream boundaries:
platform_snapshots > 3,072,299 (193,156 rows), reaction_snapshots > 1,082,322
(53,403 rows). Tail counts are verified before writes; old rows are not scanned.
Metadata 32 new rows was already loaded. Original checkpoints are NOT rewritten.
Each new batch uses the existing normal immutable snapshot writer, compares the
actual canonical metrics/reaction breakdown against those new source rows, and
commits with `migration.final_delta_checkpoint_20260907`. Original full-source
checkpoints remain incomplete, accurately recording the interrupted full pass.
Batch metadata records executionMode=delta_only and historical user waiver.

Result file after completion: `/var/lib/m-ranked/catchup-20260907/work/delta-only-result.json`.
It explicitly states fullHistoricalGatePassed=false and projectionRebuild=pending.
A succeeded batch here means the requested delta was committed and verified, NOT
that an independent full historical gate passed. Do not rerun the old full-source
bridge against this batch.

At 03:30 UTC: 27,000 new snapshots committed, 219,559 remaining; loader RSS ~38 MiB.
Collectors remain active. API and frontend smoke tests succeeded on private
loopback ports 18080/18081 and 3000: API live=200, management health=UP,
frontend=200, unauthenticated admin route=401. Public nginx still maintenance.
Original default 8080/8081 were occupied by existing nginx; target env and staged
nginx use 18080/18081. Web cache mountpoint was created in the immutable release.
Temporary API/web processes were then stopped to release resources for delta.
Authenticated admin probe was NOT performed: automatic approval rejected the
combined credential probe/service-stop command. Separate read-only checks proved
no public route used these services; their isolated stop was subsequently approved.

Interim local logical backup script/service/daily timer are staged, not enabled
or run. Backup catalogue and checksum checks are planned; no restore verification
or external copy has been established.

## Resume at 03:42 UTC — new-row delta only

The delta process stopped at 03:34:47 UTC after committing 63,000 snapshots.
PostgreSQL detected a deadlock between snapshot FK KEY SHARE locks and a native
collector deletion probe using FOR UPDATE on ingest.publication. No committed
checkpoint or row was lost.

The delta entry point now catches only DeadlockDetected, closes the rolled-back
connection, waits five seconds and reopens from its atomic delta checkpoint.
Other errors still stop visibly. Import resumed without historical replay.

The native collector deletion probe now takes FOR NO KEY UPDATE because it
changes only deleted_at. This still serializes concurrent deletion updates while
allowing foreign-key checks for new snapshots. All 45 collector tests passed.
A two-connection PostgreSQL test on two empty temporary test-schema tables
reproduced the old blocking and proved the new compatibility plus deletion
serialization. Test schema was removed. All four collectors restarted with the
fix and are active.

PostgreSQL CPU cap temporarily raised from 0.65 to 0.85 to finish catch-up while
API/frontend remain stopped; restore the normal limit before public rollout.
At 03:48 UTC: 106,000 / 246,559 new snapshots committed, ~5 GiB disk free.
The original source is not rehashed or revalidated.
