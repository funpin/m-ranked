# Production import resume — 2026-09-07

The initial import stopped at 00:25 UTC at the 3 GiB disk reserve, with
3,914,440 of 4,175,064 source rows committed. Only `reaction_snapshots` remained
incomplete (821,000 committed rows in that stream). Legacy collection continued.

The scheduled SQLite backup completed successfully at 00:26:19 UTC. To recover
space without exporting data, its exact file was compressed on the same host:

`/opt/telegram-reaction-monitor/data/backups/scheduled/reactions-20260907T002426Z.db.gz`

- Original: 1,912,782,848 bytes; gzip: 481,002,720 bytes.
- Original and fully decompressed SHA-256 both matched
  `4d167363e47ac44d555882f809c4b5d495a45c02b2942711ca151a514c5b1456`.
- The original scheduled `.db` was removed only after the compressed copy was
  fully verified and renamed into place. The adjacent `.compression.json`
  records sizes and the hash. The gzip has mode 0600 and is owned by root.
- The immutable S0 import source and live legacy database were untouched.
- Free space increased to 4,549,292,032 bytes. The compressed manual preservation
  is outside the timer's `.db` rotation pattern; account for it during later
  backup retention work. Future scheduled backups still require temporary space.

At 00:40:35 UTC, restarted the existing `m-ranked-backfill.service`, retaining
the sealed r4 source, exact original batch and all checkpoints. Started the
independent disk/query guard as transient unit
`m-ranked-backfill-progress-20260907.service`, using the existing
`/var/lib/m-ranked-migration-status/bind_progress.py`. Both the transaction guard
and independent guard retain the 3 GiB threshold.

At 00:45:38 UTC the live checkpoint counter reached 3,923,440 rows: 9,000
additional committed source rows after restart. This confirms actual resumed
writing after the repeated source inventory validation, not merely an active
systemd process. The public phase was `importing`; legacy collection was fresh.

Initial snapshot completion, reconciliation, catch-up, restore verification,
application acceptance and writer cutover remain separate outstanding steps.
This restart does not establish final migration acceptance or authorize removal
of legacy or migration evidence.

At 00:48–00:49 UTC, an additional user-authorized conservative cleanup removed
the unused `mranked-migration-runtime:20260906` image (active r2 retained),
eligible old Docker build-cache entries, apt package caches/lists, the legacy
pip download cache, and completed `/tmp/pytest-of-{root,telegram-monitor}`
trees. No apt/dpkg/pytest process was active when the latter caches were removed.
All active images, Docker volumes, browser runtimes, source artifacts, backups
and database tables remained intact. Cleanup recovered approximately 384 MB
while import continued. Repeated Docker builder pruning returned zero further
bytes; the displayed shared cache size is not additional reclaimable disk.

After cleanup, available space was 4,723,224,576 bytes (4.40 GiB), with
3,957,440 rows committed and healthy legacy collection. The remaining initial
snapshot is 217,624 rows. Capacity for projection rebuilding and subsequent
catch-up/final snapshots is not established by this cleanup.

At 00:53 UTC a further explicit cleanup of unused runtime files reclaimed
538,914,816 allocated bytes: the two installed migration-runtime tar archives
and the unused full `chromium-1234` installation. The r2 archive's layer list
and image configuration were verified equal to the active Docker image. No
process executable, cwd or open fd referred to the removed paths. The legacy
collector explicitly starts headless and its actual process executable was
inside the separate retained `chromium_headless_shell-1234` installation.
Interactive full-browser use would require reinstalling full Chromium.

The root-owned server receipt is
`/var/lib/m-ranked-migration-status/cleanup-unused-runtime-20260907.json`.
Both migration and legacy collector units remained active; the public counter
advanced to 4,003,440 at 00:53:50 UTC. Available space after removal was
5,050,159,104 bytes (4.70 GiB).

The 372 MiB `offload-20260906/old-backups.tar.gz` was retained: its previous
verification receipt names a local copy that could not be found now. It is
therefore not currently established as a disposable duplicate. The original
S0 remains mounted by the running importer and is still required for full
reconciliation and historical verification; completed per-stream checkpoints
do not establish that any portion of that SQLite file can be removed.
