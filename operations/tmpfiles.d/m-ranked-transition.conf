# Recreated at boot before any deployment or cutover operation. Runtime
# entrypoints only validate and flock this inode; they never chmod or replace it.
f /run/lock/m-ranked-transition.lock 0600 root root -
