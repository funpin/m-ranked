# Candidate only. Do not include in an active route map until release acceptance.
# Public legacy shape stays minimal/sanitized and explicitly no-store.
location = /health {
    limit_except GET HEAD { deny all; }
    proxy_pass http://m_ranked_target_api/api/v1/health/legacy;
    include /etc/m-ranked/nginx/proxy-common.conf;
}
